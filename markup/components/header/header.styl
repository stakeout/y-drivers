.page-header
	position relative
	z-index 1000
	&__inner
		display -webkit-flex
		display -ms-flex
		display flex
		flex-wrap wrap
		justify-content: space-between;
		align-items: center;
		position fixed
		left 0
		right 0
		background-color $white
